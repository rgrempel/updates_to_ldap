development:
  host: localhost
  root_dn: dc=directory_test,dc=local
  bind_dn: cn=admin,dc=directory_test,dc=local
  bind_pw: testing

# Warning: The database defined as "test" will be erased and
# re-generated when you run "rake".
# Do not set this db to the same as development or production.
test:
  host: localhost
  root_dn: dc=directory_test,dc=local
  bind_dn: cn=admin,dc=directory_test,dc=local
  bind_pw: testing

production:
  host: localhost
  root_dn: dc=directory_test,dc=local
  bind_dn: cn=admin,dc=directory_test,dc=local
  bind_pw: testing
