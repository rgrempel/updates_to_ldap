development:
  :host: localhost
  :base: dc=directory_test,dc=local
  :auth:
    :method: :simple
    :username: cn=admin,dc=directory_test,dc=local
    :password: testing

# Warning: The database defined as "test" will be erased and
# re-generated when you run "rake".
# Do not set this db to the same as development or production.
test:
  :host: localhost
  :base: dc=directory_test,dc=local
  :auth:
    :method: :simple
    :username: cn=admin,dc=directory_test,dc=local
    :password: testing

production:
  :host: localhost
  :base: dc=directory_test,dc=local
  :auth:
    :method: :simple
    :username: cn=admin,dc=directory_test,dc=local
    :password: testing
